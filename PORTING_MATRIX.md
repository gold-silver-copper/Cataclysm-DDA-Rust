# CDDA Porting Matrix

Status values: `not investigated`, `specified`, `implementing`, `implemented
but unverified`, `behaviorally verified`, `multiplayer adaptation`, and `out of
scope`.

| Subsystem or content category | Status | Evidence / next boundary |
| --- | --- | --- |
| Stable identity and allocation | Implementing | Typed 128-bit persistent-object IDs reserve 4,096 counters transactionally. Strict zero-tick `IdBlockAbandoned` then contiguous `IdBlockReserved` journal boundaries reproduce startup burns, crash gaps, paused refills, portable replay, and hourly archive ranges; replay-derived events use their own durable monotonic sequence |
| Simulation clock and phase order | Implementing | Headless tests prove commandless 20 Hz ticks, speed-100 one-second ordinary actions, signed variable-cost actor and creature movement debt, banked readiness, stable-ID ordering, a canonical two-command queue, typed overflow rejection, exact real-time crafting/book-study/disassembly/construction progress, deterministic disconnected flight, trapped defense, safe emergency nutrition, then comfortable-tile sleep before content-ranged/LOS-gated creature turns. Actor gunfire is heard before same-tick creature turns; creature structural and door-opening sound is heard in stable order for later turns. Positive content route distance selects a deterministic adjacent A* step before the existing creature action order. Once-per-second powered-tool drain, field aging, and corpse revival continue with zero players. The protocol derives and validates the pinned generic-scenario 91-day calendar plus integer solar/lunar phase, and the Bevy HUD displays it; other long activities, scenario-specific starts, and the full phase model remain |
| Map chunks and coordinates | Implementing | Canonical 12x12 chunks persist parallel per-tile pinned terrain, furniture, structural damage, and sparse type-ID-sorted multi-field state, including finalized terrain `FLAT` state, explicit field display order, intensity, and age. Tests cover furniture/terrain passability, transparency, construction flatness, accumulated cardinal/diagonal movement cost, semi-persistent structural damage and staged terrain transformation by creatures and players, furniture-before-terrain precedence, Euclidean negative coordinates, revisioned mutation, stale per-character memory without dynamic-field or live interaction leakage, open/close, collision, snapshot recovery, and deterministic edge-triggered 11x11 flat-wilderness generation; traps, vehicles, broader fields, z-levels, and CDDA mapgen remain |
| Actors, movement, collision | Implementing | Tests prove authoritative stable-ID conflict ordering plus persisted signed speed/readiness debt and queued actions. Protocol 65 carries pinned freeform STR/DEX/INT/PER creation values, independently bounded 4 through 20 with defaults of 8 and no point pool, across authenticated iroh into the crash-reconciled actor transaction; invalid simulation input is rejected before ID allocation. Horizontal movement charges the pinned source/destination average of terrain plus nonnegative furniture cost with 50 cardinal and 71 diagonal axis multipliers; ordinary floor costs 100 moves, floor-to-bed cardinal costs 175, and integer truncation is tested. Numpad plus Home/PageUp/End/PageDown expose diagonals, period/numpad 5 submits an authoritative one-second wait, and diagonal player/creature melee works. Sequence-numbered held key state uses bounded iroh datagrams, canonical replay, semantic-command priority, 100 ms refresh, and a 250 ms loss/disconnect lease. Disconnected survival autopilot spends the same movement debt to flee a visible aggressive creature within eight tiles, uses stable-ID/fixed-direction ordering, never leaves loaded passable terrain, preserves interrupted work, and reproduces through SQLite recovery/replay. Protocol 43 permits one stable-ID-selected ordinary melee defense only when no retreat increases safety; Protocol 44 permits stable ordinary emergency food/drink use only with no visible threat inside that radius and already harmful needs; Protocol 45 permits safe sleep only on the current positive-comfort furniture. Scenario/profession/trait/skill/appearance creation, field contact effects, shelter search, medicine, alarms, vertical movement, stance, stamina, encumbrance, vehicles, and other move-cost modifiers remain |
| Combat and death | Implementing | Tests prove adjacent unarmed/content-derived wielded damage, creature melee, death events, deterministic AI replay, trapped disconnected defense and continued same-tick vulnerability, SQLite/replay-equivalent defensive hits, a deterministic content-derived revolver loop, upstream-shaped `fd_blood`, and ordinary stable-ID corpses with exact overkill pulverization, carrying, damage-scaled revival, and a five-second downed state. Protocol 65 creates actors with exact freeform 4-through-20 STR/DEX/INT/PER selections. Protocol 66 applies pinned DEX/practical-melee attack speed to unarmed and strict ordinary bash weapons for player commands and disconnected defense: default unarmed costs 60 moves, while the Strength-8/DEX-8 hammer supplies smash strength 17, costs 63 moves to smash, and 74 to make an ordinary melee attack. Protocol 67 canonically retains inherited monster melee skill and dodge through corpse, revival, recovery, and replay. Protocol 68 adds deterministic hit/miss for empty-hand attacks against medium `mon_zombie` using pinned DEX/practical-melee accuracy, target dodge, a cross-platform fixed-point normal adaptation, a source-private typed miss, and identical connected/disconnected action cost. Protocol 69 extends that exact path to the admitted ordinary bash-only catalog with finalized item to-hit, pinned `MELEE_STAT` dominant-bashing selection, and practical bashing/general-melee accuracy; schema-48 recovery and portable replay reproduce the profile and outcome. Protocol 70 adds the first exact monster-side roll: a classic zombie attacks a sleeping actor's exact zero dodge, misses without damage or waking, and uses private `CLUMSY_ATTACKS` for the pinned one-in-four two-second self-down consequence. The canonical miss is not replicated because upstream suppresses sleeping-target messages; schema-49 recovery and replay retain it. Protocol 71 applies the same exact roll to every ordinary nonzero-melee-dice creature type, using finalized melee skill and preserving the pinned zero-dice early return. Protocol 72 finalizes inherited MONSTER volume, derives and canonically retains all five pinned base sizes, and applies exact target-size penalties 30/15/0/-10/-20 to empty-hand and admitted strict-bash attacks against every monster type; schema-50 recovery and replay retain nondefault sizes. Protocol 73 retains static inherited `IMMOBILE` privately through live state, corpses, revival, schema-51 recovery, and replay; it clears the full available move budget before ordinary monster interactions and adds the pinned 40-point player-hit modifier without exposing the capability publicly. Protocol 74 retains static inherited `PACIFIST` privately through corpses, revival, schema-52 recovery, and replay; it suppresses only ordinary monster melee while preserving movement. Protocol 75 finalizes inherited `attack_cost`, retains it privately through corpses, revival, schema-53 recovery, and replay, and charges its exact move value on ordinary hits and misses; signed debt and banked low-cost attacks persist/replay. Persistent furniture-before-terrain destruction, sound, recovery, and replay work; mixed/fractional/degraded/unregistered, ranged/ammunition/magazine/powered-state, and unarmed structural attacks fail closed or retain their documented temporary timing boundary. Armor, limb anatomy, other weapon hit/dodge, runtime size effects, awake monster defense, criticals, count-by-charge/live-weight timing, weapon wear, stamina/practice, full structural damage profiles, aiming/recoil, techniques, projectiles, bleeding effects, death drops, butchery/pulping/burning/rot/gibs, and rich UI remain |
| Deterministic state hashing | Implementing | Snapshot round trips preserve the ordered Postcard/BLAKE3 CanonicalStateV52 root and CanonicalEventsV15, including ordered multi-well item ownership, canonical strict-weapon to-hit, deterministic player misses, private static monster immobility, base size, and sleeping-target miss/stumble outcomes, held input, signed actor and creature movement debt, canonical STR/DEX/INT/PER, private inherited monster melee skill/dodge/clumsy/immobile/pacifist/attack-cost state, strict smash-item damage/timing profiles, queued player smash commands and typed outcomes, content-derived hearing/stumbling/bash/door-opening/routing capability, private action-counted sound and last-seen goals, terrain plus 537 admitted pinned furniture bash definitions, the complete pinned furniture bash-presence set, and per-tile damage, terrain open/close projections, per-character terrain/furniture memory and terrain flatness, sparse field types/intensity/age/display order, creature vision, self-contained corpses and downed revival state, sleep state, practical/theoretical skill progress, timed book-study, disassembly, construction, permanent learned recipes, stable installed-magazine identity, powered-tool fractional energy, reservations, selected alternatives, practice boundaries, and preallocated outputs |
| Persistence and crash recovery | Implementing | A bounded dedicated thread exclusively owns the live SQLite writer after startup and serializes journals, snapshots, checkpoints, object/account IDs, accounts, authorization, characters, reports, moderation history, and compaction. Its 64-request queue and at-most-two snapshot jobs share a 32 MiB payload budget; snapshots coalesce without losing journals. A separate read-only connection on the backup thread uses stepped SQLite online copy, keeping critical writer work live, and produces a sidecar-free replay-verified copy. Schema 54 persists ordered multi-well item ownership plus private monster base size, static immobility, pacifism, and ordinary attack cost, clumsiness and sleeping-target miss/downed outcomes, canonical strict-weapon to-hit, and private inherited monster melee skill/dodge through live state, self-contained corpses, and revival; it retains canonical STR/DEX/INT/PER and actor-dependent reading/practice inputs, the strict smash-item, field, and structural-bash catalogs, sparse field state, per-tile structural damage, content-derived hearing/visual/stumbling/bash/door-opening/routing state, deterministic player miss outcomes, player smash commands/outcomes, private sound and last-seen creature goals, and signed creature movement debt. All earlier account, object/spawn, authorization/moderation, archive, activity, terrain, item-containment/power, and ordered connection state remains. Fatal creature blood, its corpse, decay, later revival, visual behavior, sound-driven intent, route-selected pursuit, terrain door transforms, terrain-costed weighted movement, hit/miss decisions, and player/creature bashing reproduce through SQLite recovery and portable replay. Forced crash-start disconnects enter the first catch-up or live tick, making presence-dependent autopilot replayable even though connection presence remains outside the canonical hash. Hourly private format-3 archives, daily compaction, replay-verified backups, atomic restore, migration backups, WAL, hash-verified recovery, and commit-gated replies work; retained-reference catalog caching, analytical long-outage catch-up, creation-phase fault injection, and remaining administrative recovery inputs remain |
| Iroh identity and authorization | Implementing | Owner-only server/client keys, exact pending enrollment, permanent endpoint uniqueness, server pinning, and real iroh tests pass. Authenticated accounts can list bindings, stage an exact second key, prove it on the enrollment ALPN, and revoke any but the last active ID; local passwordless recovery permanently revokes old keys and recovery-locks until exact proof. Protocol 35 includes a five-minute moderator/administrator ALPN with bounded account/character/report/history/endpoint lookup, transactionally reauthorized account creation, endpoint staging/revocation, role/status changes, terminal bans, role-matrix-checked kick/mute/suspension, atomic character ownership transfer, self/last-admin/name-conflict/capacity guards, typed audit of allowed/rejected/malformed/rate-limited attempts, and account/endpoint-scoped live-session invalidation. Character lookup includes only public live-session/controlled-character metadata. A separate transactionally reauthorized administrator-only request returns one private canonical actor plus a size-checked stable-ID inventory page, count-only map memory, crafting/reading/disassembly activity, skills, and learned-recipe count; real iroh rejects moderators before state is read. Admin-created accounts and staged keys remain pending until the exact iroh identity proves itself. Players can submit five durable reports per rolling hour with bounded details excluded from the safe audit; moderators can filter and resolve reports exactly once, while history links successful sanctions to exact audit sequences. Real iroh proves account/key management and proof, report intake/lookup/resolution, history, connection/private-state authorization, mute rejection, and suspension disconnect. The macOS-compatible client exposes every implemented key/admin request as a one-shot authenticated command with copyable IDs and typed failure exits. Graphical operator UI and world operations remain |
| Network replication and interest | Implementing | Authenticated 10 Hz 11x11-submap DTOs structurally exclude canonical secrets and peer-private state. A dedicated visible-creature DTO exposes only ID, type, position, and public HP, excluding visual/sound intent, hearing flags, action debt, combat attributes, blood, and corpse reconstruction state. Terrain/furniture observations distinguish current from remembered state. Dynamic field metadata and the authoritative admitted smash-target layer appear only for currently visible tiles and never through stale memory; furniture with unsupported upstream bash behavior blocks underlying terrain metadata, and simulation still revalidates every command. Initial and later snapshots use independent bounded Zstandard iroh streams; held movement uses bounded lease-protected datagrams. Tests prove day/new-moon range changes, stale occluded decoration without field or interaction leakage, opaque terrain/furniture occlusion, admitted and unsupported furniture smash precedence, far-state filtering, creature-intent privacy, large 121-chunk bulk bounds, chat/events, real-iroh bulk delivery, held motion, and lease clearing. One session per account/character, 16-player cap, and 64 task cap work; client audible observations, full lightmaps/senses, server deltas, manifest streams, weighted-fair byte scheduling, and soak remain |
| Bevy presentation client | Implementing | Two simultaneous macOS Metal windows render authoritative chunks/actors/items and the highest-priority currently visible field using server-supplied pinned color/display order; current client also offers bounded character select/create with Up/Down-selected, Left/Right-adjusted freeform STR/DEX/INT/PER values, item, combat-target, adjacent-terrain, eight-direction H smash, crafting, book study, disassembly, and construction menus; colors/names furniture; dims remembered terrain/decor; rejects interactions through memory; renders pinned monsters and sleeping actors distinctly; issues implemented gameplay commands; and provides bounded chat plus stat, skill, and activity HUD state. Complete creation choices, field glyphs/effects, and UI remain |
| Content import and provenance | Implementing | Strict importer vendors all 7,992 tracked required-root files; hash/file-set validation passes; generated inventory covers 6,571 JSON files, 93,779 objects, 180 types, and every field. Strict partial recipe/requirement loaders retain unsupported fields and expose only definitions whose currently required semantics are complete; asset audit and most runtime semantics remain |
| World generation and overmaps | Implementing | Fresh worlds deterministically instantiate a nine-submap pinned-grass region with a floor/wall/door cabin, beds, dresser, chair, table, starter items, and zombie; movement expands persistent flat wilderness into an 11x11 active bubble without a fixed edge. CDDA overmaps, mapgen palettes/nested maps, specials, regional settings, populations, and multiple z-levels remain |
| Items, pockets, and inventory | Implementing | Registry finalizes 10,282 selected concrete ITEMs and 179 abstracts with 48 common fields plus strict pocket and transform projections, while retaining all unimplemented schema fields. Every inherited `pocket_data` object is preserved losslessly in source order with a stable index and optional source ID. Strict generalized MAGAZINE admission requires one ammunition category/capacity and no unmodeled pocket behavior; strict MAGAZINE_WELL admission resolves compatible concrete magazines and keeps all other pocket shapes fail-closed. Protocol 76 replaces the single optional well with up to 16 ordered canonical wells: installed magazines keep stable nested item IDs, reload commands/events select an exact pocket index, powered tools bind their energy pocket, deterministic tool debit spans ordered wells, disassembly detaches all installed magazines, and schema-54 recovery/replay preserve the graph. A synthetic two-well test proves independent selection and fail-closed missing-pocket behavior. Six reversible detachable-tool definitions now use modeled detach-on-disassembly rather than the empty-charge gate. Legacy integer and current grip/length/surface/balance melee `to_hit` plus inherited relative adjustments are strict; the pinned hammer resolves to -1 and the admitted ordinary bash-only profile retains it canonically. Loudness, stable ground/inventory identity, pickup/drop/wield/reload/consume/activate/read/disassemble, ammunition stacks, bounded component provenance, detachable battery identity/energy, persistent powered-light drain, recovery/replay, real iroh flow, and Bevy presentation work for their admitted subsets. A shared versioned conformance scenario proves exact direct/snapshot/SQLite/replay item-flow equivalence. G/Q/E/U/C/P/V/N use authoritative stable-ID-sorted pickers; the server remains authoritative. Item-backed ammunition and battery contents, general container/mod pockets, explicit unload/transfer commands, recharge, arbitrary power, and full item semantics remain |
| Needs, health, effects, mutations, bionics | Implementing | Integer stored calories, thirst, and pinned sleepiness thresholds advance for online/offline actors at five-minute boundaries. Voluntary/forced/autopilot sleep, accelerated recovery, halved sleeping needs, wake commands, damage/rest wakeups, unconscious perception privacy, persistence, replay, Bevy controls, and real-iroh validation are tested. Safe disconnected actors at the existing harmful starvation/dehydration threshold deterministically consume one owned unwielded ordinary `FOOD`/`DRINK`, prioritizing water and stable item ID without preempting danger. A tired actor with safe needs and no nearby visible threat sleeps only on current positive-comfort furniture. Digestion, body composition, activity/BMR, temperature/traits, medicines, sleep-location search, sleep deprivation, alarms, microsleeps, healing, health/effects, mutations, and bionics remain |
| Skills and learning | Implementing | The strict selected registry loads all 28 default pinned skill IDs and eight common fields. Actors canonically store freeform-created bounded STR/DEX/INT/PER, sorted practical/theoretical levels, raw experience, last-practiced tick, and permanent learned-recipe IDs; absent skill entries mean level zero or unknown. Default-focus crafting grants 100 raw experience per nominal second up to the pinned integer `difficulty * 1.25` cap, can raise practical and theory together, persists while disconnected, survives cancel/interruption/recovery/replay, and emits level events. Autolearn uses theoretical levels; identified carried books supply live recipe knowledge at their pinned theoretical primary-skill threshold; one-in-four completed disassembly learns eligible recipes when practical `decomp_learn` requirements pass; and crafting eligibility accepts a complete practical or theoretical requirement set. Timed study from 197 server-normalized physical skill books uses pinned level bounds, actor-INT-adjusted duration and comprehension, daylight or modeled personal/external detail light, and default-focus stochastic XP through named deterministic RNG, continues offline, and interrupts on darkness, damage, harmful needs, or exhaustion. Completed disassembly applies pinned `difficulty * 2` primary-skill practice under focus 100 with canonical INT/PER catch-up, knowledge, minimum, cap, one-level reset, and replay semantics. Identification discovery, non-default focus, helpers, ebooks, recreational reading, full lightmaps, rust, combat/other practice sources, contextual skills, traits, and level descriptions remain |
| Crafting, disassembly, and construction | Implementing | The strict selected-content registries resolve recipe requirements plus all 776 construction definitions and 438 groups while retaining unsupported reasons. Protocol 48 publishes 3,049 craft recipes, 1,227 strict disassembly definitions, and 55 constructions: 18 item-to-empty-tile furniture placements, 36 colored-carpet terrain transformations with exact floor prerequisites, and one brick-oven terrain step with AXE 2 plus CHISEL_WOOD 1. Recursive component `LIST` references use the pinned recipe dictionary and are fully expanded before journaling. Crafting and construction are server-normalized, reserve exact stable ingredients, and progress offline; construction protects quality providers and rechecks light, adjacency, occupancy/`FLAT` or exact terrain. Disassembly uses pinned defaults or exact provenance, unloads strict ranged targets, gates unmodeled power, detaches every modeled magazine, and excludes creature corpses. These activities survive CanonicalStateV52 snapshots, schema-54 recovery, portable replay, replication, and private inspection. General construction tool-charge consumption, other specials, broader terrain chains/deconstruction, item-backed ammunition/power, step recipes, workspaces/full lightmaps, batches, containers, stochastic failure, and remaining definitions remain unavailable |
| Vehicles | Not investigated | Characterize pinned upstream behavior |
| Monsters, NPCs, factions, dialogue, missions | Implementing | MONSTER registry finalizes 1,177 selected definitions and 33 abstracts with inherited volume, melee skill, dodge, materials, flags, vision, hearing, bash, door-opening, and the full observed `path_settings` object. Protocol 70 canonically retains `CLUMSY_ATTACKS` through live creatures, corpses, and revival and uses it for the pinned one-in-four two-second fall after an exact classic-zombie miss against sleeping zero dodge. Protocol 71 broadens that sleeping-target roll to every ordinary nonzero-melee-dice creature while preserving the upstream zero-dice early return. Protocol 72 derives all five pinned volume-based sizes, retains size privately through corpses/revival/replay, and applies exact size penalties to admitted player attacks without expanding the public DTO. Protocol 73 canonically retains inherited static `IMMOBILE`, clears full readiness before ordinary attacks/opening/bashing/movement, and adds the exact 40-point player-hit modifier. Protocol 74 retains static inherited `PACIFIST` and suppresses only ordinary adjacent melee while preserving pursuit and movement. Protocol 75 finalizes inherited `attack_cost`; ordinary melee hits and misses spend the exact content value, with signed debt and banked low-cost multi-attack behavior. Stable creatures acquire only living actors inside content-derived visual range and terrain/furniture LOS, tie-break by stable actor ID, persist and pursue the exact last-seen destination until arrival, and otherwise may follow private sound intent from same-z gunfire, structural bashing, and terrain-door opening. Normal and `GOODHEARING` perception, deterministic interest/imprecision/replacement, action-counted lifetime, and visible/remembered/sound priority are verified through recovery and portable replay. Positive path distance selects a deterministic same-z A* adjacent step over loaded passable terrain, admitted doors, and registered terrain/furniture bash targets; zero preserves greedy movement. Search uses pinned neighbor order, padding, cost budget, movement/diagonal/door/dangerous-field costs, pinned bash-rating costs, base-strength route estimates, and deterministic multiplayer tie-breaks. Actual group-bash hits still use connected helpers. The candidate fan then applies content-derived `STUMBLES` weighting and Q30 terrain/furniture movement debt before attacking disconnected characters. The default zombie has distance zero and performs deterministic `BASHES`/`GROUP_BASH` attempts against registered terrain and furniture: furniture takes layer precedence, removal/replacement preserves floor, and the final cabin frame resolves to floor. The strict runtime registry admits 537 of 699 pinned furniture definitions, including every fresh-cabin furniture tile, while the full upstream bash-presence set prevents unsupported layers from exposing terrain. Capable pinned feral-human types carry `CAN_OPEN_DOORS`, path distance 45, and route-open doors. Opening emits `swish` at volume 6, pays zero moves, and may immediately enter. Capabilities and path policy survive corpses/revival/replay. The Bevy client renders only the authoritative public creature projection; interior-only transforms, route caching/backoff, vertical/trap/sharp/nearby-creature hazard routing, client/player hearing, scent, camouflage/effects, unsupported bash side effects/dynamic-floor repair, furniture/vehicle doors, unlocking, other movement flags, death drops/spawning, and all NPC/social systems remain |
| Weather, fields, seasons, inactive catch-up | Implementing | The tick-derived generic 91-day calendar and pinned Boston solar/lunar boundaries are deterministic and downtime ticks persist them. Protocol 49 loads pinned field intensity/display/lifetime data, persists sparse multi-field tiles, creates creature-specific blood on ordinary death, and ages it once per second with fixed-point exponential or exact linear decay. Fire/fuel, smoke/gas spread, contact effects, underwater/outdoor acceleration, weather, seasonal behavior, warm/dormant analytical catch-up, and scenario-specific starts remain |
| Included core data and bundled mods | Implementing | All pinned files are vendored; generated inventory includes 238 `field_type` definitions across 15 files. Default loaders cover 47 MOD_INFO records, 158 ammunition types, 10,282 selected ITEMs, 1,177 selected MONSTERs, 1,246 terrain definitions, 699 furniture definitions, 28 skills, 234 proficiencies, more than 5,000 recipes, 1,428 core uncraft definitions, and 474 reusable requirements. Field types now have a strict partial registry; MONSTER implements 25 inventoried fields and retains 87 unsupported. ITEM, terrain, furniture, field processors, the other definition types, and most gameplay integration remain |
| English source strings and UI | Specified | Pinned English-only scope |
| Non-English catalogs/runtime language switching | Out of scope | Not reproducible from pinned Git commit |
| Mobile, browser, console, Android, iOS | Out of scope | Explicit initial-platform exclusion |
